/target
**/*.rs.bk
Cargo.lock.*
.DS_Store
*.log
.idea/
.vscode/
/tests/fixtures/_build/

# Internal-only docs (kept on disk locally; not part of the published repo).
/TAPE_V0_BRIEF.md
/DECISIONS.md

# A test snapshot left over from manual /tape:tape-snapshot runs.
/test-snapshot.tape

# Claude Code project-local config (dev-side agents, skills, commands, hooks).
# Useful locally; not part of the distribution.
/.claude/
