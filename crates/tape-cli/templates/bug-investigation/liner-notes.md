## What I was asked to do

{{task}}

## What I found

Generated from the `bug-investigation` template. The 12-track shape walks a typical bug hunt: read the suspect modules, ask the model to reason about them, query the relevant data, confirm the hypothesis against one more file, annotate the smoking gun, write the fix in two files, and eject. Replace this paragraph with the real finding before sharing the cassette.

## Suggested next step / fix

Generated from the `bug-investigation` template — replace with the concrete next step (e.g. "add an advisory lock keyed on `customer_id` around `process_refund()`").

## What I'm uncertain about

Generated from the `bug-investigation` template — list open questions here (e.g. "whether adjacent flows share the same lock domain").
